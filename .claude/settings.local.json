{
  "permissions": {
    "allow": [
      "Bash(npm create *)",
      "Bash(npm install *)",
      "Bash(rm -rf src)",
      "Bash(mkdir -p src/components src/pages src/hooks)",
      "Bash(rm -rf *)",
      "Bash(mkdir -p components pages hooks)"
    ]
  }
}
